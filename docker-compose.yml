services:
  frontend:
    build:
      context: .
      dockerfile: ./Dockerfile # React's Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./server
      dockerfile: ./Dockerfile # Server's Dockerfile
    ports:
      - "5001:5000"